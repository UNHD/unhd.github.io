# 参考与第三方资源

- **RhineLabUI** — https://github.com/LBEILC/RhineLabUI 。沿用音效 `src/audio.ts` 与标题反馈 `src/scrub-title.ts`；档案阵列、阻尼、选择波、抽取与归位交互参考该项目，并适配石蒜模型。参考项目未发现根目录通用源码许可证，本仓库不替其声明额外授权。
- **MiSans** — 字体文件及许可位于 `public/fonts/`，见 `NOTICE.txt` 和 `MiSans-license.pdf`。
- **Rolling Number 0.4.1** — 许可位于 `public/licenses/rolling-number.txt`。
- **Three.js** — MIT 许可位于 `public/licenses/three.txt`。
- **Vite、TypeScript、Prettier** — 开发工具按 `package-lock.json` 锁定版本，遵循各包附带的许可。

石蒜 GLB、石蒜主题终端布局及演示档案为本项目制作。青色 C# Analyzers 档案归档了此仓库原有的首页与 `Directory.Build.props`，保留原配置中的版本、注释和许可字段。

交互所参考的原 PV 为《明日方舟》特别映像「莱茵生命：访问」（BV1rr4y1b7sz）。原作品名称、标志与设定的权利归各自权利人；本项目不分发原片。
